# Live Code server configuration

LIVE_CODE_HOST=127.0.0.1
LIVE_CODE_PORT=5050
FLASK_DEBUG=1

# Optional: nudge the agent when todos remain open at turn end
# LIVECODE_TODO_GATE=1

# LLM API keys are configured in the in-app Settings panel
# and stored in ~/livecode/settings.json
